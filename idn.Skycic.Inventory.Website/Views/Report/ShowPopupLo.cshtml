@model List<Inv_InventoryBalanceLot>
@{
    var ProductCode = ViewBag.ProductCode;
    var ProductCodeUser = ViewBag.ProductCodeUser;
    var ProductName = ViewBag.ProductName;
}
<!--Model Add-->
<div class="modal-dialog" style="width: 1000px; max-width: 1000px;">
    <div class="modal-content">
        <div class="modal-header">
            <h2 class="modal-title" id="exampleModalLabel"><b>Danh sách lô tồn kho</b></h2>
            <a href="javascript:;" onclick="ClosePopupLo()" class="close" aria-label="Close"><span aria-hidden="true">&times;</span></a>
        </div>
        <div class="modal-body" style="">
            @using (Html.BeginForm(null, null, FormMethod.Post, new { id = "manageFormShowPopupLot" }))
            {
                @Html.AntiForgeryToken()
                <div class="col-12 no-padding">
                    <h2 class="modal-title">@ProductCodeUser - @ProductName</h2>
                </div>
                <div class="col-12 no-padding layout-bot-top-16">
                    <div class="scrollable-horizontal padding-left-0 padding-right-0" style="padding-top: 0!important;">
                        <div style="width: 100%; float: left;">
                            <table id="dynamic-table-thead" class="table table-bordered table-thead table-cus table-bottom-0 no-margin">
                                <thead>
                                    <tr class="trthead">
                                        <th class="text-center cell-100">@("Số lô".HtmlItemString("inventory"))</th>
                                        <th class="text-center cell-75">@("Ngày sản xuất".HtmlItemString("inventory"))</th>
                                        <th class="text-center cell-75">@("Ngày hết hạn".HtmlItemString("inventory"))</th>
                                        <th class="text-center cell-50">@("Số lượng tồn".HtmlItemString("inventory"))</th>
                                        <th class="text-center cell-75">@("Vị trí tồn".HtmlItemString("inventory"))</th>
                                    </tr>
                                </thead>
                            </table>
                        </div>
                        <div id="scrollable_1" class="scrollable" style="width: 100%; float: left">
                            <table id="dynamic-table-thead-tbody" class="table table-borderedtable-cus table-bottom-0 dynamic-table-tbody dynamic-table-thead-tbody no-margin">
                                <tbody id="Lst_InvF_MoveOrdInstLot">
                                    @{
                                        if (Model != null && Model.Count > 0)
                                        {
                                            foreach (var item in Model)
                                            {
                                                <tr class="trdata">
                                                    <td class="text-center cell-100">@item.ProductLotNo</td>
                                                    <td class="text-center cell-75">@item.ProductionDate</td>
                                                    <td class="text-center cell-75">@item.ExpiredDate</td>
                                                    <td class="text-center cell-50 numberic1 valTotalOKPopup">@item.QtyTotalOK</td>
                                                    <td class="text-center cell-75">@item.InvCode</td>
                                                </tr>
                                            }

                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            }
        </div>
        @*<div class="modal-footer">
                <a class="btn font-color mybtn-Button" href="javascript:void(0);" onclick="SaveInvBalanceLot()" style="float: right;font-size: 13px;"><i class="fa fa-plus-circle" aria-hidden="true"></i> Hoàn tất</a>
            </div>*@
    </div>
</div>
